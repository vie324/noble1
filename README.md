# Noble — ノーブル業務統合システム

エステサロン「ノーブル」（新宿店・新宿南口店・恵比寿店）の業務統合システム。
カルテ・来店記録・回数券（KaruteKun代替）、シフト・共有カレンダー（TimeTree代替）、
経営ダッシュボード、同意書・カウンセリングの電子化、掲示板、在庫管理を1つに集約しています。

## 技術スタック

- Next.js 16（App Router）+ TypeScript + Tailwind CSS v4
- Supabase（Postgres / Auth / Storage / RLS）
- デプロイ想定: Vercel（または任意の Node ホスティング）

## セットアップ手順

### 1. Supabase プロジェクトの作成

1. https://supabase.com で新規プロジェクトを作成
2. SQL Editor で `supabase/migrations/` の SQL を **番号順（001 → 024。欠番あり）**に実行
   - 各ファイル冒頭にロールバック手順を記載しています

### 2. 店舗とオーナーアカウントの作成

SQL Editor で `supabase/setup-production.sql` を実行すると、店舗3件と
オーナーアカウント（管理者）が作成されます。

| ロール | メール | パスワード |
|---|---|---|
| 管理者 | marin@noble.example.com | noble-marin-2026 |

メール・パスワードは SQL ファイル冒頭の変数で変更できます。
**運用開始後に必ずパスワードを変更してください。**
スタッフの追加はログイン後「経営 > マスタ管理 > スタッフ」と、
Supabase Authentication でのユーザー作成（メールを一致させる）で行います。

### 3. アプリの起動

```bash
cp .env.example .env.local   # URL と anon key を記入
pnpm install
pnpm dev                     # http://localhost:3000
```

## 画面一覧

| ルート | 画面 | 権限 |
|---|---|---|
| `/login` | サインイン | — |
| `/` | 今日ボード（本日のご予約・未記入アンバー強調・日付送り） | 全員 |
| `/visits/new` | 来店予定（空カルテ）の作成。顧客検索＋新規顧客の最低限登録 | 全員 |
| `/visits/[id]` | カルテ入力（メニュー・部位・回数券消化・写真・メモ・📌重要事項） | 全員 |
| `/visits` | カルテ一覧（期間・担当・未記入で絞り込み、ページング） | 全員 |
| `/customers` | 顧客検索（名前・カナ・電話の部分一致インクリメンタルサーチ） | 全員 |
| `/customers/[id]` | 顧客ページ（フラグ・申し送りが最上部／回数券／来店タイムライン／LINE導線） | 全員 |
| `/calendar` | 共有カレンダー（月表示。「🐨 10-19」チップ縦積み・定期タスク・変更は赤強調・予定追加） | 全員 |
| `/calendar/my` | マイシフト（希望提出 ○/×/時間/いつも通り/お任せ・確認済みチェック・勤務実績の記録） | 全員 |
| `/calendar/manage` | シフト管理（募集開始/締切・希望を見ながらグリッドでドラフト作成→一括確定・未確認者・予定vs実績・**報酬/店舗別交通費の自動計算・実績の月締め（日別修正/未記録日の一括記録）・社労士/税理士向けCSV出力**） | 管理者 |
| `/admin/dashboard` | 経営サマリー（KPI・売上構造グラフ・媒体別新規。未消化残高はカルテ実データ連動） | 管理者 |
| `/admin/sales` | 実績入力（日次売上。回数券販売・消化は「連動値を反映」でカルテから転記可） | 管理者 |
| `/admin/analytics` | 売上・媒体分析（CPA自動計算・キャッシュイン vs 役務消化の推移） | 管理者 |
| `/admin/menus` | メニュー・部位分析（カルテ実データから自動集計。部位×月のクロス集計） | 管理者 |
| `/admin/staff-performance` | スタッフ実績（売上・目標・指名・リピート率。スタッフには非公開） | 管理者 |
| `/admin/simulation` | 目標シミュレーション | 管理者 |
| `/admin/tickets` | 回数券未消化残高 KPI・要フォロー一覧 | 管理者 |
| `/admin/masters` | マスタ管理（メニュー・部位・フラグ・回数券商品・カウンセリング項目・同意書テンプレ・商品/備品・スタッフ） | 管理者 |
| `/admin/stores` | 店舗紹介ページの確認（公開URLのコピー・掲載内容の確認・プレビュー） | 管理者 |
| `/admin/line` | 店舗別 LINE Messaging API 連携（チャネルトークン設定・Webhook URL・接続テスト） | 管理者 |
| `/api/line/[storeId]` | 店舗別 LINE Webhook（友だち追加・メッセージ受信。署名検証） | LINE専用 |
| `/intake` | カウンセリング受付（LINE等から届いた未紐付け回答を確認→既存顧客に紐付け or 新規登録→カルテ作成） | 全員 |
| `/board` | 掲示板・資料置き場（カテゴリ・ピン留め・添付ファイル。投稿は管理者） | 全員 |
| `/inventory` | 在庫管理（理論在庫・入庫登録（納品書画像）・月末棚卸と差分理由・消費量設定） | 全員（消費量設定は管理者） |
| `/help` | スタッフ向け使い方ガイド（LINE設定手順を含む） | 全員 |
| `/documents/[id]/print` | 署名済み同意書の表示・印刷（ブラウザ印刷でPDF保存可） | 全員 |
| `/salons` | **お客様向け 店舗一覧・ご予約ページ（固定URL・LINEリッチメニュー「ご予約」の遷移先）** | 認証不要 |
| `/salons/[code]` | **店舗紹介ページ**（住所・アクセス・地図・道案内・営業時間・ネット予約導線。`shinjuku` / `shinjuku-south` / `ebisu`） | 認証不要 |
| `/f/new` | **お客様向けカウンセリング受付フォーム（固定URL・LINEリッチメニュー「カウンセリング」の遷移先）** | 認証不要 |
| `/f/c/[token]` `/f/s/[token]` | **お客様向け公開フォーム**（個別カウンセリング／同意書の電子署名。LINEでリンクを送付） | 認証不要（トークンURL） |
| `/p/[token]` | **ビフォーアフター公開ページ**（お客様共有用。顧客ページから作成・LINE送付） | 認証不要（トークンURL） |
| `/api/ics?key=…` | Googleカレンダー購読用 iCal フィード（`ICAL_FEED_SECRET` で保護） | シークレットURL |

> 公開フォームはサーバー側で service role を使って読み書きします。
> `.env.local` に `SUPABASE_SERVICE_ROLE_KEY` の設定が必要です。
> LINEリッチメニューの設定手順は [`docs/LINE_SETUP.md`](./docs/LINE_SETUP.md) を参照してください。
>
> 店舗紹介ページ（`/salons`）の掲載内容（住所・営業時間・道案内・電話番号など）は
> [`src/lib/salons.ts`](./src/lib/salons.ts) にまとめています。DBを触らずにこのファイルだけで
> 更新でき、店舗コードは `stores.code` と一致させています。地図はAPIキー不要の
> Googleマップ埋め込みを使用しています。

全画面共通でヘッダーに「全店舗 ⇔ 新宿店 ⇔ 新宿南口店 ⇔ 恵比寿店」のワンタップ切替があります。

### 4. 旧システム（カルテくん）のデータ取り込み（任意）

顧客・カルテ・回数券の履歴を CSV から取り込めます。
`supabase/migrations/023_karutekun_import.sql` を実行したうえで、
CSV を取込テーブルへ入れて `select * from public.import_karutekun();` を実行します。
手順は [`docs/DATA_IMPORT.md`](./docs/DATA_IMPORT.md) を参照してください。

## 動作確認手順（受け入れ条件）

1. **朝の箱作成（30秒以内）**: 今日ボード →「＋来店予定を作る」→ 顧客名の一部を入力 → 候補タップ → 時間チップ → 担当チップ →「この内容で箱を作る」
2. **未記入の即判別**: 今日ボードで左端がアンバーの行＝未記入。上部カードに未記入件数
3. **カルテ入力（1〜2分）**: 未記入行をタップ →「カルテを記入する」→ メニュー・部位をチップで選択（「前回と同じ」で複製可）→ 回数券をタップで1回消化 →「記入を完了する」
4. **回数券の整合性**: 消化→顧客ページ・`/admin/tickets` の残回数・未消化金額が即時一致。「取り消す」で復元
5. **要注意顧客**: フラグを立てた顧客を開く → フラグ＋申し送りが最上部に表示
6. **店舗切替**: ヘッダーのタブをタップ → 今日ボード・一覧・残高がすべて切替
7. **権限**: スタッフ（misaki 等）でログイン → `/admin/tickets` に直接アクセスしても `/` へリダイレクト。API レベルでも RLS と `is_admin()` で遮断
8. **レイアウト**: iPad 縦・横、スマホ（ボトムナビ）で崩れないこと
9. **店舗紹介ページ**: 未ログインのスマホで `/salons` を開く → 3店舗が並ぶ →
   「店舗の詳細・地図を見る」→ 地図・道案内・ネット予約ボタンが表示される。
   管理者は「経営 > 店舗紹介ページ」でURLをコピーできる

## データベース・ロールバック

各マイグレーションの冒頭コメントにロールバック SQL を記載しています。
全削除する場合は番号の大きい順（014 → 001）で実行してください。

## セキュリティ設計

- 全テーブルで RLS 有効。`staff` 行が存在する有効なログインユーザーのみアクセス可
- マスタ変更・回数券の直接修正・各種削除は管理者のみ
- 回数券の消化／取消は行ロック付きの `use_ticket` / `cancel_ticket_usage` 関数経由（二重消化防止・監査証跡）
- スタッフ別の分析データ（リピート率・販売実績）は管理者専用（RLS で読み取り自体を遮断）
- 時間給・店舗別交通費（シフトの報酬自動計算用マスタ）も管理者専用（RLS で読み取り自体を遮断）
- お客様向け公開フォーム（/f/...）は anon に RLS を開放せず、サーバー側で service role による
  トークン検証付きの読み書きのみ
- 施術写真は非公開バケット＋署名付きURL（クライアント側で圧縮してからアップロード）
